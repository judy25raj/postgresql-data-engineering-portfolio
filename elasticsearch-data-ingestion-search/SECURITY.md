# Security Note
This project uses `src/hidden_template.py` as a credential template.

✅ Do NOT commit `src/hidden.py` or any `.env` with real credentials.
`.gitignore` already blocks common secret files.
