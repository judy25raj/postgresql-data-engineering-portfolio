
# Project Overview

This project demonstrates **relational normalization** using PostgreSQL across three datasets:
- Automobiles (manual inserted dataset)
- Musical Tracks (CSV → album/track)
- UNESCO Heritage Sites (CSV → normalized tables)

The purpose is to convert **raw, denormalized text data** into **fully normalized relational tables** with:
- Foreign key relationships
- Unique constraints
- Clean joins
